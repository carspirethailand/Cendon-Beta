# สิ่งที่ฝั่งเซิร์ฟเวอร์ต้องทำเพิ่ม — ระบบช่าง และ Gemini ค้นเว็บ

หน้าเว็บทำเสร็จและใช้งานได้แล้วโดยไม่ต้องรอเซิร์ฟเวอร์
สองเรื่องด้านล่างคือส่วนที่ต้องมีปลายทางจริง ถึงจะสมบูรณ์

---

## 1) `/api/ai/stream` ต้องรับ `system` และ `search`

**นี่คือสาเหตุที่ Gemini บอกว่าค้นหาไม่ได้**

ทางสตรีมคือทางที่ถูกใช้จริงเกือบทุกครั้ง (`/api/ai/chat` เป็นทางสำรอง
ที่จะถูกเรียกก็ต่อเมื่อสตรีมล้มเหลว) แต่ของเดิมหน้าเว็บไม่เคยส่ง
`system` กับ `search` ไปกับทางสตรีมเลย — ฝั่งหน้าเว็บแก้แล้ว
ตอนนี้ส่งไปครบทุกรอบ

ฝั่งเซิร์ฟเวอร์ต้องอ่านสองค่านี้จาก body ของ `/api/ai/stream`
แล้วส่งต่อให้ Gemini แบบเดียวกับที่ `/api/ai/chat` ทำอยู่แล้ว

```
body: { contents, system, search, temp, carId, make, model, year, mileage, skillIds, userName }
```

- `system` → `systemInstruction`
- `search:true` → ใส่ `tools:[{google_search:{}}]`

ถ้าไม่ทำข้อนี้ โมเดลจะยังตอบว่าค้นเว็บไม่ได้เหมือนเดิม
และจะไม่รู้จักข้อมูลรถของผู้ใช้กับข้อมูลช่างที่หน้าเว็บแนบไปให้

---

## 2) ปลายทางของระบบช่าง

ตอนนี้คลังช่างอยู่ในไฟล์ `techs.js` ของหน้าเว็บ ค้นหาได้จริงทั้งหน้า
`techs.html` และ AI ในหน้าแชต แต่แก้ไขจากแผงควบคุมไม่ได้
และใบสมัครยังค้างอยู่ในเครื่องของคนสมัคร

ต้องมีสองปลายทาง

### `GET /api/tech`
คืนช่างที่อนุมัติแล้วทั้งหมด — เปิดให้ทุกคนอ่านได้ ไม่ต้องล็อกอิน

```json
{ "techs": [ { "id":"t20","name":"...","shop":"...","area":"...",
  "lat":13.8,"lng":100.6,"cats":["eng"],"rating":4.8,"jobs":120,
  "years":9,"from":500,"to":9000,"reply":15,"verified":true,
  "mobile":true,"urgent":false,"brands":["Toyota"],"warranty":30,
  "skills":["..."],"about":"..." } ] }
```

รูปแบบนี้ต้องตรงเป๊ะกับที่ `techs.js` ใช้ เพราะหน้าเว็บเอาไปรวมกับ
ชุดในเครื่องด้วย `Techs.merge()` — ฟิลด์ไหนขาด การ์ดจะแสดงว่าง

### `POST /api/tech/apply`
รับใบสมัคร — คนสมัครยังไม่ได้ล็อกอินก็ควรส่งได้

```json
{ "id":"app_...","name":"","shop":"","phone":"","age":30,"area":"",
  "years":5,"port":6,"from":500,"warranty":30,
  "cats":["eng"],"mobile":true,"urgent":false,"about":"" }
```

หน้าเว็บตรวจเกณฑ์ทุกข้อให้แล้วก่อนส่ง แต่ **ฝั่งเซิร์ฟเวอร์ต้องตรวจซ้ำ**
เพราะการตรวจในเบราว์เซอร์คือความสะดวก ไม่ใช่ความปลอดภัย
เกณฑ์บังคับดูได้จาก `Techs.RULES` ที่ `req:true`

ใบสมัครควรเข้าคิวรออนุมัติแบบเดียวกับ `/api/skills/pending`
แล้วให้แผงควบคุมกดอนุมัติ ก่อนจะไปโผล่ใน `GET /api/tech`

ถ้ายังไม่มีปลายทางทั้งสองอัน หน้าเว็บจะทำงานต่อได้ตามปกติ
ใบสมัครถูกเก็บไว้ในเครื่องรอส่งใหม่ ไม่หายไปไหน

---

## 3) ระบบเงินพักและค่าธรรมเนียมลดหลั่น (รอบที่ 4)

หน้าเว็บอธิบายกติกาไว้ครบแล้วที่ปุ่ม "ความคุ้มครอง" ในหน้าช่าง
ตัวเลขและเงื่อนไขทั้งหมดอยู่ที่ `Techs.TRUST` ใน `techs.js`
ฝั่งเซิร์ฟเวอร์ต้องทำให้มันเป็นจริง มิฉะนั้นมันเป็นแค่คำสัญญา

### สิ่งที่ต้องมี

- **เงินพักไว้ (escrow)** — ลูกค้าจ่ายเข้าระบบ เงินอยู่กับระบบจนกดยืนยันว่างานเสร็จ
  ต้องมีสถานะงานอย่างน้อย: `quoted → accepted → working → done → paid`
  และ `disputed` ที่หยุดการจ่ายไว้จนคนกลางตัดสิน
- **ค่าธรรมเนียมนับตามคู่ (ลูกค้า, ช่าง)** ไม่ใช่ตามงาน
  งานที่ 1 = 10% · งานที่ 2 = 6% · งานที่ 3 ขึ้นไป = 3% · ลูกค้าที่ช่างพามาเอง = 0%
  ต้องเก็บตารางว่าคู่ไหนทำงานกันมากี่ครั้งแล้ว
- **เบอร์กลาง (number masking)** — ต้องใช้บริการภายนอก เช่น Twilio หรือผู้ให้บริการในไทย
  ทั้งสองฝ่ายเห็นแต่เบอร์กลาง เบอร์จริงไม่เคยหลุดออกจากฐานข้อมูล
- **กรองเบอร์และไอดีไลน์ในแชต** เฉพาะก่อนสถานะ `working`
  หลังจากนั้นต้องปล่อยให้คุยกันได้ตามปกติ เพราะช่างต้องโทรนัดเวลาจริง
- **ประกันงาน** — ผูกกับ `job_id` ที่จ่ายผ่านระบบเท่านั้น
  งานที่ไม่ได้จ่ายผ่านระบบต้องไม่มี job_id ให้เคลม

### หมายเหตุสำคัญ

การกรองเบอร์ในแชตกันได้แค่คนที่ไม่ตั้งใจ คนที่ตั้งใจจะพิมพ์เลขไทยหรือเว้นวรรคก็ผ่านแล้ว
**อย่าลงแรงกับการกรองมากกว่าที่ควร** — ชั้นที่ได้ผลจริงคือค่าธรรมเนียมที่ลดลง
เมื่อกลับมาใช้ซ้ำ กับประกันงานที่หายไปถ้าออกไปข้างนอก
ทั้งสองอย่างนั้นทำงานโดยไม่ต้องจับใคร

## 4) เกณฑ์คัดช่างสี่ด่าน

`Techs.VET` มีรายละเอียดครบทั้งสี่ด่าน ด่านที่ต้องมีคนทำจริง:

- ด่าน 1 ตรวจบัตรประชาชนคู่ใบหน้า — **ต้องลบภาพภายใน 30 วัน** ตาม PDPA
- ด่าน 2 ตรวจหนังสือรับรองมาตรฐานฝีมือแรงงานแห่งชาติ สาขาช่างซ่อมรถยนต์
  กับกรมพัฒนาฝีมือแรงงาน หรือจัดสัมภาษณ์ทางเทคนิคแทน
- ด่าน 3 ไปดูอู่จริง
- ด่าน 4 โทรหาลูกค้าห้าคนแรกหลังปิดงาน

ด่าน 4 เป็นด่านเดียวที่เป็นอัตโนมัติได้ทั้งหมด — ที่เหลือต้องใช้คน
ควรวางแผนกำลังคนไว้ก่อนเปิดรับสมัครจริง
